en:
  date:
    formats:
      default: '%d-%b-%Y'
      long: '%B %d, %Y'
      short: '%b %d'
      with_weekday: '%a %m/%d/%y'
      long_without_year: '%d %B'

  time:
    formats:
      default:
        '%a, %b %-d, %Y at %r'
      date:
        '%b %-d, %Y'
      short:
        '%B %d'
